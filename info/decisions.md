- chose : for assignment to prevent common misconception that '=' means equals
  and not one-sided assignment to a memory location in programming.
- set <> as a sampling operator instead of random() as a function call because
  of native stochasticity
