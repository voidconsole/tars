## comments
Single line comments ``` :) ```
Multiline comments ```:()```


## functions
fnc hello(param) {
	return `{param}hi`
}

## variable
str(or txt?) myString = "text"
num myNum = 34
num myNumF = 35.64
arr myArray = [hello, 34, 54.3 , world, [ nest] ]
obj myObj {
	key: value; 
}

mat rowLy = <
[hi, welp, bye ],
 [hi erl, ret, null],
 [sup, chat, how]
 >
 
 dst colLy = < head1:[ key1, key2, ke3]; head2:[mey1, mey2,me3]>

flx bool = true
flx boll = false
num fuzz = 30 ~ 5 
flx truth = maybe  
flx truth = maybe 0.3

## operators
+ _ * / ^ ~ ! 
+= -= *= ^= /= != ~= 
-> (pipeline)