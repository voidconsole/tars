let location = [];
// Sepereate raw datat on blobkcs of data and blocks of data based on lines. 
function lex(input) {
    const tokens = [];
    const lines = input.split("\n");
    return tokens;
}
export default lex;
